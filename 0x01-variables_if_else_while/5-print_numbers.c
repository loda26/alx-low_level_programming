#include <stdio.h>

/**
 * main -Entry point
 *
 * Description: print numbers of base 10 starting from 0.
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	for (int i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	return (0);
}
